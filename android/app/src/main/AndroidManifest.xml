<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Events V2 Near Me Phase N2.1 — foreground-only, coarse location.
         Near me filters against a fixed 100km radius (see
         GeolocatorCurrentLocationProvider's own LocationAccuracy.low
         choice), which needs neither meter-level precision nor
         background access — ACCESS_FINE_LOCATION and
         ACCESS_BACKGROUND_LOCATION are deliberately NOT declared. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <application
        android:label="Mantelier"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Password recovery App Link — mirrors the iOS Associated
                 Domains entry (applinks:mantelier.app, path
                 /auth/reset-password). autoVerify="true" will NOT actually
                 verify yet: that requires a /.well-known/assetlinks.json on
                 mantelier.app listing this app's release-signing SHA-256
                 fingerprint, which doesn't exist until Android release
                 signing is set up. Until then this filter is inert — Android
                 falls back to its disambiguation dialog instead of opening
                 the app directly, never a crash or a broken link. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="mantelier.app"
                    android:pathPrefix="/auth/reset-password"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
