# Gault&Millau Dataset — Validation Control Report

Generated by `validate_gault_millau.py` (read-only, no database connection; re-runnable by anyone against this folder's CSVs plus `supabase/data/restaurants_master.csv`). **Regenerated 2026-08-11 after the production-readiness review's match reclassifications** (see `PRODUCTION_READINESS_REVIEW.md` §4).

**Row counts:** 87 restaurants, 87 matches, 63 special awards, 20 sources, 774 existing catalogue restaurants (`restaurants_master.csv`).

**Match classification tally (post-review):** NO_MATCH 30 · EXACT 52 · HIGH_CONFIDENCE 4 · REVIEW 1.

| Check | Result |
|---|---|
| No duplicate (name, city, country, guide_year) rows in `gault_millau_restaurants.csv` | OK |
| `gm_candidate_id` unique across `gault_millau_restaurants.csv` | OK |
| All non-null scores fall within 0–20 | OK |
| All non-null toque counts fall within 0–5 (incl. Germany's "N red/black Hauben" format) | OK |
| All `guide_year` values in `gault_millau_restaurants.csv` fall within 2020–2026 | OK |
| All `guide_year` values in `gault_millau_special_awards.csv` fall within 2020–2026 | OK |
| No `gault_millau_url` reused across two different restaurant names (excluding the "not found" null placeholder) | OK |
| `source_id` unique across `gault_millau_sources.csv` | OK |
| `gault_millau_matches.csv` row count equals `gault_millau_restaurants.csv` row count (87 = 87) | OK |
| `existing_restaurant_code`: present for all EXACT/HIGH_CONFIDENCE, absent for all NO_MATCH (REVIEW may carry one as unconfirmed context) | OK |
| No REVIEW-tier row appears in the EXACT/HIGH_CONFIDENCE matchable set that `import_gault_millau.py` uses | OK |
| Every `existing_restaurant_code` resolves against `restaurants_master.csv` (774 rows) — FK feasibility | OK |
| `gault_millau_new_restaurant_candidates.csv` is exactly the NO_MATCH set, no more no less | OK |
| Every `gault_millau_restaurants.csv` row carries `source_url`, `retrieval_date`, `confidence` | OK |
| Every `gault_millau_special_awards.csv` row carries `source_url`, `retrieval_date` | OK |

**TOTAL: 15 OK, 0 ISSUES**

## REVIEW-tier row (new since the original control report)

`gm_024` ("212", Amsterdam) — downgraded from HIGH_CONFIDENCE during the production-readiness review after a genuine address conflict was found: the GM source gives Amstel 212, 1017 AH Amsterdam; the catalogue gives Amsteldijk 212, 1079 LK Amsterdam — different streets, different postal codes, several kilometers apart. The name match alone ("212" vs "Restaurant 212") is not sufficient to resolve this. It still carries `existing_restaurant_code = rest_0085` for context, but `import_gault_millau.py` structurally excludes REVIEW rows from its matchable set (verified by an automated check in this report), so it cannot be imported by accident.

## Per-market classification breakdown

| Market | Total | EXACT | HIGH_CONFIDENCE | REVIEW | NO_MATCH | Matchable (EXACT+HC) |
|---|---|---|---|---|---|---|
| Austria (AT) | 12 | 11 | 0 | 0 | 1 | 11 |
| Belgium (BE) | 16 | 8 | 1 | 0 | 7 | 9 |
| Switzerland (CH) | 10 | 7 | 0 | 0 | 3 | 7 |
| Germany (DE) — **deferred, excluded from launch** | 15 | 12 | 3 | 0 | 0 | 15 |
| France (FR) | 19 | 1 | 0 | 0 | 18 | 1 |
| Netherlands (NL) | 15 | 13 | 0 | 1 | 1 | 13 |
| **Launch scope (FR+BE+NL+CH+AT)** | **72** | **40** | **1** | **1** | **30** | **41** |

## Notes beyond the automated checks

- **FK feasibility was independently confirmed live, twice**, not just via this CSV-only script: `import_gault_millau.py --dry-run` and `--apply` (local dev database only, cleaned up after) resolved all 41 launch-scope matchable `restaurant_code`s to real `id`s, confirmed idempotent on re-run, and separately confirmed the `--include-deferred-markets` override correctly adds exactly the 15 German award rows + 5 German special-award rows on top (56/63 totals).
- **No fabricated values found** — every score/toque/URL cell absent from source data is either empty (CSV null) or an explicit `"not found"`-style placeholder with a reason, never a guessed number.
- **This report validates data quality and internal consistency only.** It does not and cannot validate whether a restaurant should be inserted into the catalogue — see `PRODUCTION_READINESS_REVIEW.md` §5–6 for that separate, human-judgment-dependent question (the `restaurants.inclusion_reason` architectural gap in particular).
